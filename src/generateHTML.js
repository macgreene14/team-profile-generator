fs = require("fs");

function generateHTML(employees) {
  // accept classes, generate cards, insert into template, save local
  console.log(employees);
  console.log("HTML Generated!");
}

module.exports = generateHTML;
